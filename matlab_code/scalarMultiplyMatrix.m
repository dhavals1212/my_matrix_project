function resultMatrix = scalarMultiplyMatrix(matrix, scalar)

  resultMatrix = matrix * scalar;
%scalarMultiplyMatrix multiplies each element of a matrix by a scalar.
% resultMatrix = scalarMultiplyMatrix(matrix, scalar)
% matrix: Input matrix (e.g., [1 2; 3 4])
% scalar: The scalar value to multiply by
end
